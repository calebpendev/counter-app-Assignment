import React, { Component } from 'react';

function TopCounter() {
    return (
        <header style={topCounterStyle}>
            <h1>Counter Placeholder</h1>
        </header>
        )
}

const topCounterStyle = {
    background: 'blue'
}

export default TopCounter;